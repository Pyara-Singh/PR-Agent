apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  PR_AGENT_ENVIRONMENT: {{ .Values.config.environment | quote }}
  PR_AGENT_DATABASE_URL: {{ required "config.databaseUrl is required" .Values.config.databaseUrl | quote }}
  PR_AGENT_QUEUE_BACKEND: "kafka"
  PR_AGENT_KAFKA_BOOTSTRAP_SERVERS: {{ required "config.kafkaBootstrapServers is required" .Values.config.kafkaBootstrapServers | quote }}
  PR_AGENT_QDRANT_URL: {{ required "config.qdrantUrl is required" .Values.config.qdrantUrl | quote }}
  PR_AGENT_S3_BUCKET: {{ required "config.s3Bucket is required" .Values.config.s3Bucket | quote }}
  PR_AGENT_S3_ENDPOINT_URL: {{ .Values.config.s3EndpointUrl | quote }}
  PR_AGENT_CORS_ORIGINS: {{ required "config.corsOrigins is required" .Values.config.corsOrigins | quote }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
type: Opaque
stringData:
  PR_AGENT_GITHUB_WEBHOOK_SECRET: {{ required "secrets.githubWebhookSecret is required" .Values.secrets.githubWebhookSecret | quote }}
  PR_AGENT_GITHUB_TOKEN: {{ .Values.secrets.githubToken | quote }}
  PR_AGENT_S3_ACCESS_KEY: {{ .Values.secrets.s3AccessKey | quote }}
  PR_AGENT_S3_SECRET_KEY: {{ .Values.secrets.s3SecretKey | quote }}
